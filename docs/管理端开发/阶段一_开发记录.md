# 阶段一开发记录

## 开发基线

- 日期：2026-08-07
- 仓库：D:\Desktop\MVP\MVP-Qian-backend
- 分支：test
- HEAD：6c3a66c (Route diagnosis and mixed responses through LLM for detailed 5-layer expansion)
- 远端 test：6c3a66c (同步)
- 工作区状态：已 stash 用户修改 (pre-stage1-user-changes)，工作区干净

## 规则与方案

已读取：
- D:\E盘\实验\rule\CLAUDE.md
- D:\E盘\实验\rule\EXAMPLES.md
- D:\Desktop\管理端功能开发方案(1).md
- MVP-Qian-backend\AGENTS.md
- MVP-Qian-backend\agent.md (本次新建)

## 当前代码现状

- 前端：web/index.html + web/app.js + web/styles.css + web/graph-renderer.js
- 后端：app/server.py (Python HTTP server) + app/services/ (auth, feedback, etc.)
- 身份：纯客户端选择 student/admin，localStorage 存储，服务端无校验
- 管理入口：跳过测评，复用学生端完全相同界面
- 岗位数据管理：位于 workspace #workspaceJobAdmin tab，所有角色可见
- tool-drawer：右侧面板共享，无管理员独立工作台

### 已知冲突

- AGENTS.md 原文写"不搭建登录、权限、班级管理、数据库"，阶段一需要建立教师角色基础
- 处理方式：记录此冲突，阶段一只做最小角色区分（student/teacher），不改造完整认证体系

## 本次修改

完成了管理端阶段一全部 7 项任务：

1. **任务1 - 教师角色基础**：`auth.py` 新增 `teacher_required()` 函数，种子用户 000 为 `teacher` 角色，JWT 已包含 role
2. **任务2 - 角色分流**：登录后根据服务器返回的 `role` 自动确定身份，教师跳过身份选择直达岗位选择
3. **任务3 - 教师端页面骨架**：右侧 tool-drawer 分为 student/teacher 两个版本，教师端有 5 个模块入口
4. **任务4 - AI 定位调整**：教师端 Topbar 标题改为"机电岗位培训 AI · 教师端"，输入框 placeholder 改为教师自然语言查询
5. **任务5 - 岗位数据迁移**：`岗位图谱` 模块映射到现有 `#workspaceJobAdmin` 面板，复用全部岗位数据管理功能
6. **任务6 - 清理学生端**：学生 tool-drawer 移除"岗位数据"按钮，workspace tabs 中"岗位数据" tab 对学生隐藏
7. **任务7 - 预留结构**：为班级洞察、学生管理、教学评语、教师工作台建立 workspace 占位面板，显示"该功能将在后续阶段开放"

## 修改文件

| 文件 | 修改内容 |
|---|---|
| `app/services/auth.py` | 新增 `teacher_required()`，种子 user 000 设为 teacher |
| `app/server.py` | 新增 `/api/user/role`，更新 `/api/identity` 返回角色，教师端点加权限保护 |
| `web/index.html` | Landing overlay 改为教师入口，tool-drawer 分 student/teacher，workspace 加教师面板和占位 |
| `web/app.js` | 登录流处理教师角色，`showRoleUI()` 切换界面，workspace 标题和面板映射添加教师模块 |
| `docs/管理端开发/阶段一_开发记录.md` | 本次新建 |

## 验证

### 服务端验证
- POST /api/auth/login (user 000) → role: "teacher" ✓
- POST /api/auth/login (user 001) → role: "student" ✓
- POST /api/user/role (teacher token) → role: "teacher" ✓
- GET /api/teacher/summary (student token) → 403 ✓
- GET /api/teacher/summary (teacher token) → 200 ✓

### 前端结构验证
- role-student / role-teacher CSS 类正确应用于 tool-drawer 和 workspace panels ✓
- 教师 workspace 占位面板（教师工作台、班级洞察、学生管理、教学评语）✓
- 岗位图谱 → teacherJobGraph 映射到 #workspaceJobAdmin ✓
- 学生端移除 jobAdmin tool-drawer 按钮 ✓

## 已知问题

- user 000 首次登录需等数据库重建；如已有旧 users.db 需手动删除以触发重新 seed
- 当前教师端不验证 job_role 权限范围，任何教师可查看所有班级数据（后续阶段完善）

## 回退点

- Archive 0：阶段一开发前基线，commit `6c3a66c`
- Archive 1：角色和页面结构拆分完成（待提交）

## 下一步

整体回归验证学生端已有功能。