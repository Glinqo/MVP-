# TF-6A: V2 Backend Real-chain Closure, Workflow Persistence, Full Ownership Audit

**Branch**: ix/teacher-frontend-boot-roleui
**Start SHA**: 36440ad
**Final SHA**: 1a5864c
**Date**: 2026-08-09

---

## Completed Phases

### Phase A: V2 Facade Audit
All 17 public methods classified and connected to real engines.

### Phase B+D: Persistent Intervention Workflow Store (SQLite)
- data/v2_workflow.db — persistent SQLite store with tables: interventions, targets, tasks, outcomes
- Strict FSM with valid transitions enforced
- create_intervention, 	ransition_intervention, get_intervention, list_interventions, get_student_interventions all operational

### Phase E+F: Outcome tied to real intervention evidence
- get_outcome() reads from persistent outcome store
- evaluate_intervention() uses OutcomeEvaluator with real pre/post states

### Phase I: Student ownership enforcement
- equire_student_owner() middleware function in pp/middleware.py
- Enforced on /api/graph/student, /api/student/events, /api/student/bootstrap POST

### Phase M: Governance Permissions (NEW)
Added 	eacher_required guards to 3 previously unprotected governance endpoints:
- /api/graph/job/ingest
- /api/graph/job/proposals/confirm-sqlite
- /api/graph/job/proposals/confirm-sqlite-batch

### Phase G+H: Teacher AI V2 Real Chain (NEW)
- Rewrote 	eacher_ai_v2.py — all methods now route through V2 Facade
- ind_teaching_issues: calls real discover_issues() from V2 Facade
- get_intervention_status: reads from persistent workflow_store
- draft_intervention: uses persistent workflow_store
- generate_intervention_candidates: gets ability_id from real issue
- get_process_patterns: calls real PatternClassifier
- get_student_state: calls real LearnerState
- Added handle_teacher_message_v2() handler with V2 intent routing
- /api/teacher/assistant/message now routes V2-first, falls back to V1 for unsupported intents

### Phase N: Conversation Ownership (NEW)
- All conversation endpoints now check JWT ownership
- list_conversation_sessions() accepts owner and job_role parameters for filtering
- Students can only access their own conversations
- Teachers can access conversations within their job_role scope
- Write operations (rename, delete) enforce ownership

### Phase T: Ban Silent Fake Fallback (NEW)
- Removed get_issue() returning fake {"status": "unknown"} data
- Returns explicit {"status": "not_found", "error": "ISSUE_NOT_FOUND"} for non-existent issues
- 	eacher_ai_v2.py fully rewritten without silent exception swallowing

### Phase P: API Error Contract (NEW)
- All error responses use send_error_json(status, message) with proper HTTP codes
- No 200 responses with {"ok": false, "error": "forbidden"} patterns found

---

## Verified

- Workflow: create → review → assign → reload confirms persistence
- Illegal transition (assigned → draft) correctly rejected
- All 6 modified files pass Python compilation
- Engine smoke test: V2 Facade, Teacher AI V2, Workflow Store, Middleware all operational

---

## File Changes

| File | Change |
|------|--------|
| pp/server.py | Governance guards (Phase M), Teacher chat V2 routing (Phase G+H), Conversation ownership (Phase N) |
| pp/services/teacher_ai_v2.py | Complete rewrite — real engines, ban fake fallback, V2 intent handler |
| pp/services/v2_facade.py | get_issue returns explicit error instead of fake data (Phase T) |
| pp/services/conversation_state.py | list_conversation_sessions accepts owner/job_role filter params (Phase N) |
| pp/services/issues/__init__.py | Fixed syntax error (malformed triple quote) |
